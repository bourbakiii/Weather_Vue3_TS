export type City = {id:string,name:string, geo_lat: string, geo_lon: string};


export type UserData = {
    ip: string | null;
    city: City | null;
  };
  
  export type Option = {
    
  }